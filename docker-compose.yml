version: '3'

services:
  app:
    image: talitafesan/webacademy:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "${APP_PORT}:8080"
    environment:
      NODE_ENV: production